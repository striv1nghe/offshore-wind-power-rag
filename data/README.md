# Data Directory

This project reads local CSV files from this folder.

Expected structure:

```text
data/
├── INPUT/
├── PRED_RESULT/
├── METRIC_RESULT/
└── LOSS_DATA/
```

The real CSV files are ignored by Git because they may contain private research or operational data.
